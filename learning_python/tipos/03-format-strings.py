# pylint: disable=missing-module-docstring, invalid-name

nombre = "Matias"
apellido = "Velazquez"
# nombreCompleto = nombre + " " + apellido
nombreCompleto = f"{nombre[0]} {2 + 5}"  # f = operador de formateo de string
print(nombreCompleto)
